<li>
  <p><%= task.content %></p>
  <% if task.is_executed %>
    <p>実施済み</p>
    <p><%= task.executed_at %></p>
  <% else %>
    <p>未実施</p>
  <% end %>
  <% if user_signed_in? %>
    <%= link_to '編集する', task_path(id: task.id) %>
  <% end %>
</li>
